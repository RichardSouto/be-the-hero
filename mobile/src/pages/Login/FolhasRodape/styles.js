import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    containerFolhas: {
        flexDirection: "row",
        justifyContent: 'space-between', 
        width: 500,
        position: 'absolute', 
        bottom: -50, 
        left: -30
    }
})